<Важно>!
Очень долгим и опытным путем было установлено, что в методе
	(Класс ChooseAdmin) loader.findClass(name,f);	
и далее внутри
	defineClass(name, memory, 0, memory.length);
вызывается исключение.
Как оказалось также, об этом исключении (SecurityException)
явно сказано в документации к этому методу.
Из документации:
"SecurityException - If an attempt is made to add this class 
to a package that contains classes that were signed by a different 
set of certificates than this class (which is unsigned), 
	or if name begins with "java."
	
Добавлено:
+ проверка на содержание слова "java"
+ обработка исключения

Изменения в классе Server:
! - теперь сервер открывает сокет не при инциализации класса,
	а при непосредственном запуске метода run()
	Остальное поведение осталось нетронутым

Изменения в DAdminInterface:
! - разграничил класс админа и класс его интерфейса
	Интерфейс ничего не должен совершать над классом админа,
	не должен им управлять. Админ создает интерфейс.
	Новый конструктор интерфейса
		public DAdminInterface(DAdmin a) {
			admin = a;
			initComponents();
		}
	Ссылка на админа остается у интерфейса.

! - класс first переименован в MainStarting
! - добавлен класс GeneralMesgs для будущего хранения команд.
	рассмотреть его полезность

	Сервер запускается, клиенты нормально коонектятся.
	Есть серьезная проблема с производительностью - после подключения двух клиентов
	процессор загружен полностью. Надо срочно в этом разобраться.
	После старта игры:
		Одному из клиентов приходит сообщение turn/+, и больше ничего.
		У другого же не видно вообще ничего.
		Возможно, проблема с клиентом, он не отображает следующие пришедшие команды.
		Но это очень странно, потому как обработка сообщений все также ведется в отдельном
		потоке.
		
14.01.2012
	! - удален пакет начального тестового проекта netCards
14.01.2012
	В классе DurakInteface
	+ - отображение карт игрока
	+ - отрисовка игрового стола
	